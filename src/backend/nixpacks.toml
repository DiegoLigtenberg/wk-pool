# poetry.lock triggers Poetry 1.3 by default, which breaks this PEP 621 project.
# Use pip/setuptools install into Nixpacks' /opt/venv instead.
[variables]
NIXPACKS_PYTHON_VERSION = "3.12"
NIXPACKS_PYTHON_PACKAGE_MANAGER = "setuptools"

[phases.build]
cmds = []
